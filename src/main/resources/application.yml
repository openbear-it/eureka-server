server:
  port: ${PORT:8761}
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
management:
  endpoints:
    web:
      exposure:
        include:
          - info
          - metrics
          - prometheus
spring:
  application:
    name: ${APP_NAME:eureka-server}

